---
description: Do not leak or mishandle secrets when env or credential files are in scope
globs: "**/.env*,**/secrets/**,**/*credential*,**/*.pem"
alwaysApply: false
---

# シークレット

- これらのファイルの**実値**を会話に出さない。必要ならキー名のみや `.env.example` 形式で示す。
- コミットにシークレットを含めない。誤って含めた場合はローテーションを促す。
